"use strict";
let No_Users = [];
if (No_Users.length == 0) {
    console.log("We need to find some Users!");
}
else {
    console.log();
    //Great Users
}
;
//Removing all usernames ensures the
let messages = "We need to find some users!";
console.log(messages);
